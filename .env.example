# Bucket and write configuration — uncomment one block at a time
# No AWS_PROFILE because it shouldn't be baked into the Docker image.
# TODO: We can just explicitly set bucket in argo. The commands already accept it as an overwrite param.

# # Source.Coop
# BUCKET=us-west-2.opendata.source.coop

# # Auspatious LDN custom domain
# BUCKET=data.ldn.auspatious.com

# DEP Staging
BUCKET=dep-public-staging

# # DEP Production
# BUCKET=dep-public-data
